# Timeslices

An timeslice is a duration and always bound to an activity.

## GET /apidoc/timeslices

```
{
  "id": "integer",
  "activity_id": "integer",
  "user_id": "integer",
  "duration": "integer",
  "started_at": "datetime",
  "stopped_at": "datetime"
}
```
